apiVersion: v1
kind: Service
metadata:
  name: pyapp-service
  namespace: dev
  labels:
    app: pyapp
spec:
  selector:
    app: python
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
