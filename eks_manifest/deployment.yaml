apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-deployment
  namespace: dev
  labels:
    app: python
spec:
  replicas: 3
  selector:
    matchLabels:
      app: python
  template:
    metadata:
      labels:
        app: python
    spec:
      containers:
      - name: python
        image: kevinjcloud/myapp:v1
        ports:
        - containerPort: 8000

