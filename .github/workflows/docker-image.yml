name: K8S  CI

on:
  push:
    branches: [ "main" ]
  
jobs:

  pyapp-ci:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: "docker build -t kevinjcloud/pyapp-gha:$GITHUB_SHA ."
